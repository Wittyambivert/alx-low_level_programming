#include <ctype.h>
#include "main.h"


/**
 * _isupper - function to print uppercase check
 * @c: - for betty
 * Return: 0
 */

int _isupper(int c)
{
	if (isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
